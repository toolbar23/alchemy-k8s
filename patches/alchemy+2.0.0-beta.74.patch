diff --git a/node_modules/alchemy/lib/Hetzner/actions.js b/node_modules/alchemy/lib/Hetzner/actions.js
--- a/node_modules/alchemy/lib/Hetzner/actions.js
+++ b/node_modules/alchemy/lib/Hetzner/actions.js
@@ -34,8 +34,8 @@ const backoff = Schedule.min([
 /**
  * Poll a Hetzner Action until it reaches `success` or `error`.
  *
- * Bounded: at most 10 polls, exponential backoff starting at 500ms and
- * capped at 5s (under 60s total). Already-finished actions return
+ * Bounded: at most 60 polls, exponential backoff starting at 500ms and
+ * capped at 5s (under 5 minutes total). Already-finished actions return
  * immediately.
  */
 export const waitForAction = (ref) => Effect.gen(function* () {
@@ -56,7 +56,7 @@ export const waitForAction = (ref) => Effect.gen(function* () {
         return action;
     })), Effect.retry({
         while: retryable,
-        times: 10,
+        times: 60,
         schedule: Schedule.min([
             Schedule.exponential(Duration.millis(500), 1.5),
             Schedule.spaced(Duration.seconds(5)),
diff --git a/node_modules/alchemy/src/Hetzner/actions.ts b/node_modules/alchemy/src/Hetzner/actions.ts
--- a/node_modules/alchemy/src/Hetzner/actions.ts
+++ b/node_modules/alchemy/src/Hetzner/actions.ts
@@ -62,8 +62,8 @@ const backoff = Schedule.min([
 /**
  * Poll a Hetzner Action until it reaches `success` or `error`.
  *
- * Bounded: at most 10 polls, exponential backoff starting at 500ms and
- * capped at 5s (under 60s total). Already-finished actions return
+ * Bounded: at most 60 polls, exponential backoff starting at 500ms and
+ * capped at 5s (under 5 minutes total). Already-finished actions return
  * immediately.
  */
 export const waitForAction = (
@@ -96,7 +96,7 @@ export const waitForAction = (
       ),
       Effect.retry({
         while: retryable,
-        times: 10,
+        times: 60,
         schedule: Schedule.min([
           Schedule.exponential(Duration.millis(500), 1.5),
           Schedule.spaced(Duration.seconds(5)),
